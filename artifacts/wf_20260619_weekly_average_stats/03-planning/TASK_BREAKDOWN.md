# 주간 평균 통계 작업 분해

- Workflow ID: `wf_20260619_weekly_average_stats`
- Stage: `task_planning`
- Producing agent: `default` (planner 역할 수행)
- Source task ID: `task_weekly_planning_001`
- Timestamp: `2026-06-19T00:00:00+09:00`
- Summary: 주간 평균 통계를 도메인 계산, UI, 테스트와 검증 단계로 분해한다.
- Inputs used: `01-product/FEATURE_SPEC.md`, `02-architecture/TECH_DESIGN.md`
- Open assumptions: 제품 정의 승인 후 기존 앱 구조 안에서 구현

## 1. 작업 순서

| 순서 | 작업 | 쓰기 범위 | 완료 조건 | 예상 |
| --- | --- | --- | --- | --- |
| 1 | 제품 정의 승인 | 승인 기록 | 주 시작일·분모·표시 기간 확정 | 0.25일 |
| 2 | 날짜 경계 유틸리티 | `src/domain/**` | 월·연도·윤년 경계 테스트 통과 | 0.5일 |
| 3 | 주간 집계 모델과 함수 | `src/domain/**` | 평균·표본·비교·8주 시리즈 테스트 통과 | 0.75일 |
| 4 | 통계 화면 리팩터링 | `src/features/insights/**`, `src/app/App.tsx` | 계산 로직이 화면에서 분리됨 | 0.5일 |
| 5 | 주간 요약 UI | `src/features/insights/**`, `src/styles/**` | 3개 평균 카드와 전주 대비 표시 | 0.75일 |
| 6 | 8주 추이 UI | `src/features/insights/**`, `src/styles/**` | 빈 주·동일값·결측값 안전 표시 | 0.5일 |
| 7 | 통합 테스트 및 검증 | `src/test/**`, 테스트 보고서 | 빌드·테스트·린트·360px 확인 통과 | 0.75일 |

총 예상: 약 4 개발일

## 2. 세부 태스크

### T01. 제품 결정 승인

- 월요일 시작을 승인한다.
- 열량과 운동 평균을 저장된 일일 로그 수로 나누는 규칙을 승인한다.
- 최근 8주와 표본 1회 표시 정책을 승인한다.

### T02. 주간 날짜 유틸리티

- 문자열 날짜 파싱 및 포맷 함수를 구현한다.
- 기준 날짜가 속한 월요일과 일요일을 계산한다.
- 주 단위 이전·다음 날짜 이동을 구현한다.
- 월말, 연말, 윤년 테스트를 작성한다.

### T03. 주간 통계 도메인

- `WeeklyStatistics`, `WeeklyComparison` 타입을 추가한다.
- 주별 평균 체중, 열량, 운동 시간을 계산한다.
- 전주 대비 델타를 계산한다.
- 최근 8주 시리즈를 오래된 순서로 생성한다.
- 빈 값, 미래 날짜와 정렬되지 않은 입력을 테스트한다.

### T04. 통계 화면 구조 개선

- `InsightsScreen`을 기능 디렉터리로 이동한다.
- 기존 `slice(-range)` 계산을 제거한다.
- 주간 계산 결과를 `useMemo`로 생성한다.
- 기존 기록 화면 및 내비게이션 회귀를 방지한다.

### T05. 요약 카드와 비교 표시

- 평균 체중, 평균 섭취 열량, 평균 운동 시간 카드를 구현한다.
- 전주 대비 값과 중립적인 증가·감소 문구를 표시한다.
- `기록 4/7일`, `체중 3회` 표본 정보를 표시한다.
- 데이터 부족 및 비교 불가 상태를 구현한다.

### T06. 최근 8주 시각화

- 주 시작 날짜 레이블과 평균 체중을 표시한다.
- 빈 주는 값 없음으로 유지한다.
- 축 범위가 동일한 경우를 안전하게 처리한다.
- 360px에서 레이블 충돌과 가로 넘침을 확인한다.

### T07. 품질 게이트

- 도메인 및 컴포넌트 테스트를 추가한다.
- 빌드, 전체 테스트와 린트를 실행한다.
- 실제 브라우저에서 모바일·데스크톱 화면과 키보드 접근을 확인한다.
- 테스트 보고서와 리뷰 보고서를 갱신한다.

## 3. 완료 정의

- 기능 요구사항의 모든 수용 기준을 테스트 또는 브라우저 검증으로 확인한다.
- 이번 주·지난주 및 최근 8주가 동일한 주간 경계 함수를 사용한다.
- 빈 데이터에서 `0kg`, `NaN`, `Infinity`가 노출되지 않는다.
- 기존 8개 테스트를 포함한 전체 테스트가 통과한다.
- 최종 diff가 주간 통계와 필요한 화면 구조 개선 범위를 벗어나지 않는다.

